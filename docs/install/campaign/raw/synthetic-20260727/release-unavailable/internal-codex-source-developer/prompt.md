# Initial task

You are a fresh developer installing NQ from documented distribution paths on a clean Linux environment. Work only inside /tmp/nq-install-operator-specimen-f853180/scenarios/release-unavailable. Read SCENARIO.md and only the files it names. Do not inspect repository source, architecture notes, other scenarios, or hidden implementation. Use terminal reads as needed and report every command/file consulted. Follow documentation literally: do not invent assets, copy developer binaries, or silently repair the environment. Determine what failed, what changed, what remains safe, and the supported next action. Return one JSON object matching /tmp/nq-install-operator-response.schema.json exactly. No prose outside JSON.

# Format-only follow-up

You may read `/tmp/nq-install-operator-response.schema.json` solely as the response-format contract. It contains no scenario expectations. Do not inspect anything else beyond SCENARIO.md and its named evidence files.
