You are a fresh central operations engineer who wants only NQ's monitor/dashboard and will connect remote sources later. Work only inside /tmp/nq-install-operator-specimen-f853180/scenarios/monitor-only. Read SCENARIO.md and only the files it names; you may also read /tmp/nq-install-operator-response.schema.json solely as the response format. Do not inspect repository source, architecture notes, other scenarios, or hidden implementation. Use terminal reads as needed and report every command/file consulted. Do not invent missing services or infer health from an empty screen. State exactly what to install/run and what the result does and does not establish. Return one JSON object matching the schema exactly. No prose outside JSON.
