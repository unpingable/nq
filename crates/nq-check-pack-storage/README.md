# nq-check-pack-storage

This optional pack owns the executable ZFS, SMART, and NVIDIA GPU observation
families. It is not a default: helpers, privileges, hardware, and
platform-specific interpretation must be selected explicitly.

The pack preserves raw witness reports and collection failures. It does not
turn helper success into evidence of operational safety or an NQ disposition.
