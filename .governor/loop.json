{
  "reconciled_note": "RECONCILED 2026-06-23 (operator-directed). This file had gone stale at 2026-06-18T19:33Z: it predated the commit of state-schema-reconciliation (e8b3c94) and the four active-witness TLS slices 2a-2d-a (2026-06-19), plus the 2026-06-23 NQ render/canon/specimen packets. current_slice/blocked_on/slice_spec now reflect idle; the previously-in-flight and since-landed work is moved into completed_slices. Local-substrate reconciliation only; no push.",
  "phase": "AUDIT",
  "overnight_mandate_2026_07_03": "OPERATOR-RATIFIED overnight run (plan ~/.claude/plans/linear-honking-prism.md; receipt 2026-07-03T0638Z.overnight-mandate-and-slice0-gap-status-repair.json). Queue: slice0 gap-status repair [DONE]; slice1 scrape-target provenance persistence (WIRE IT per operator); slice2a-2f SILENCE_UNIFICATION detector migrations (additive); slice3a-3c service_state completeness (witness_packet feed / served_surface_registry / docker native); stretch blackbox Bucket 1. Codex = blind reviewer per code slice. Push allowed at clean checkpoints. NO deploy/host mutation. Silence ladder: 2x same failure -> park; 2 consecutive parks -> stop + morning report. NOT authorized: WITNESS_CLAIM_SCOPE, C2 dual-marker follow-ons, basis-stale audit, Lane B/C, new gap authoring.",
  "current_slice": "*** DAYLIGHT DEPLOY 2026-07-03 (holiday, operator-cleared) \u2014 ChatGPT order steps 1-4 DONE. (1-3) THREE-HOST FLEET DOCKER DEPLOY: sushi-k canary -> NAS -> VM, schema 60->63 (commit 38d0167; migrations 061 tombstones/062 retirement/063 zfs-history), per-host AGGREGATOR-FIRST restart (nq-serve before nq-publish). Docker native capture LIVE in prod: sushi-k governor-code-adapter running+healthy; VM driftwatch running+healthy, caddy+pds running+NULL (no-healthcheck path). All GREEN: dash 200, PUBLIC nq.neutral.zone 200, boundaries present, leak 0. Receipt 2026-07-03T1622Z.three-host-deploy-schema63-docker-native.json. (4) BLACKBOX BUCKET 1 PROVE-THE-WHEEL: blackbox_exporter v0.28.0 live on sushi-k (user-systemd, promoted nq_aggregator_http_localhost http_2xx) + VM (system-systemd, promoted external_http_nq_neutralzone https public-path; valid LE cert observed). Both smoke_probe.sh SMOKE PASS, collision=0. nq-blackbox @ e768f63 PUSHED (unpingable/nq-blackbox). Receipt 2026-07-03T1642Z.blackbox-bucket1-deploy-prove-the-wheel.json. LIVE FINDING: blackbox /probe emits BARE probe_success -> NQ series identity (metric_name,labels_json) collapses 2 targets/host to collision=1, so ONE clean bare-probe_success target per island. PARKED next levers (operator to choose, un-opened): (a) scrape-target IDENTITY migration NQ_SCRAPE_TARGET_IDENTITY_SCOPE = unblocks multi-probe-per-host blackbox (code slice, not deploy); (b) ChatGPT order STEP 5 paper layer = WITNESS_CLAIM_SCOPE / C2 dual-marker follow-ons / basis-stale audit (new code surfaces, deferred until deploy receipts exist \u2014 they now do). Loop AUDIT/idle, parked per operator 2026-07-03. *** \n--- PRIOR: idle \u2014 overnight-2026-07-03 run CLOSED (receipts 0638Z/0659Z/0712Z). Landed: slice0 gap-status repair (63eec06) + slice3c docker native-state capture w/ codex blind review (9f04f7f, PUSHED). Reconciled already-shipped: slice1 provenance (058), slice3a/3b (SUPPORT_CUTOVER). Parked: slice2 SILENCE_UNIFICATION (operator hold \u00a72a), blackbox Bucket 1 (sole remaining gate = blackbox_exporter deploy; provenance + nq-serve now cleared). Daylight operator-gated: exporter deploy session; fleet deploy of docker collector (AGGREGATOR FIRST \u2014 named ordering constraint in FEATURE_HISTORY); unselected batches un-opened. PRIOR: idle \u2014 Packet A (active declared-deny subject probe) EXECUTED 2026-06-25 (36ada5d). Built a real pfSense CE 2.7.2 lab on sushi-k (libvirt/KVM, vantage VM behind pfSense) and observed ALL SIX live faces of nq.probe.declared_deny.v1 against a controlled deny rule (alias nq_lab_deny_v4=8.8.8.8): declared_deny_observed_blocked (primary), _observed_reachable (contradiction via deliberately shadowed rule), cannot_testify_declared_policy_absent, _probe_target_unbound, declared_deny_probe_inconclusive, cannot_testify_vantage_unbound. Teardown captured (rule gone -> absent; subject reaches again; control reached throughout). Specimen docs/specimens/declared_deny_lab_subject_probe.md (+ plan executed). NO probe code changed (frozen from check #1). Receipts gitignored runs/declared-deny/lab-2026-06-25/ (RFC1918+benign IPs; paranoia clean). Custody: official Netgate serial memstick UNMODIFIED + documented manual install; recipe/hashes in local ~/nqlab/custody-artifacts/. Step-0 pfSense now CLOSED with all three checks AND check #1 promoted from blocked-only to full observed_blocked specimen.",
  "slice_spec": [
    "Packet 3 / Phase 1 (operator-directed, reframed to inventory-first): tree out pfSense read surfaces and SOURCE-TYPE them (declaration / runtime report / historical report / observed-reachability), sketch the declared reachability graph, name candidate drift checks. Doc: docs/working/decisions/PFSENSE_REACHABLE_DRIFT_STEP0_INVENTORY.md.",
    "First specimen pure core (offline): crates/nq-monitor/src/lease_presence_probe.rs \u2014 nq.probe.lease_presence.v1, clock-injected, compares a pfSense DHCP lease report against presence observations (ARP residue / NQ probe from a named vantage). Non-lift ladder: corroborated / uncorroborated / cannot_testify_no_presence_basis / lease_expired_or_absent. Refusals are the product (lease != presence; uncorroborated != host-down). Receipt-only, no coercion. 11 offline fixtures.",
    "Operator decisions captured: read path = SSH + pfctl (key-only, LAN/VPN-only, read-only); first check = lease-vs-presence as a cheap non-lift. SNMP queued as a later RuntimeReport channel.",
    "GATED next slice (NOT started): live SSH read \u2014 needs operator to enable read-only SSH on the pfSense LAN gateway + a named vantage. ISC-vs-Kea lease detection; arp -an; optional ICMP/TCP presence probe from the vantage."
  ],
  "acceptance": "PASS. Offline: full cargo test -p nq-monitor --no-fail-fast EXIT=0 (lease_presence probe+transport incl. parsers, ISC state mapping, ARP, append-only). Live (REVIEW): read-only SSH to pfSense (ISC dhcpd backend detected), real lease+ARP read + ICMP from the LAN vantage; observed both lease_corroborated_by_presence and the lease_uncorroborated non-lift against real subjects (receipts under gitignored runs/, real host data not committed). No mutation, no SSH writes, no scheduler.",
  "lane": "operator-directed (research specimen, render/copy + pure verdict core)",
  "authorization_mode": "Standing Conditional Authorization \u2014 operator approved classes (completeness repair / paper-built / probe promotion / doc-code reconciliation / local-governance-substrate); loop executes matches until policy/external/irreversibility/ambiguity, then refers. Idle now; the 2026-06-23 NQ packets + reconciliation were operator-directed.",
  "blocked_on": "nothing in flight \u2014 pfSense lease-presence Phase 1 (3599c85) + Phase 2 (68f9c61) committed (no push). Next packets parked; open only on explicit operator direction (no free-jazz).",
  "sca_batch_done": [
    "doctrine 333da0d; provenance-persistence 1bd5c38 (+collision guard); blackbox harness nq-blackbox dcb8e66; series-identity REFERRED (scope packet); SILENCE_UNIFICATION classified Lane A, scope packet, first slice named",
    "Receipts: 2026-06-12T1730Z.sca-batch-close-audit.json"
  ],
  "silence_batch_done": [
    "smart_witness_silent + zfs_witness_silent silence contract SHIPPED (d955578, persist-derivation per OQ1); also REPAIRED batch-A migration-058 masked failures (CURRENT_SCHEMA_VERSION 57->58, upgrade-test constants, publish !metric_sets guard). Verified green with REAL exit codes (the masking scar is now in loop-protocol \u00a7 REVIEW).",
    "Four non-witness silence detectors still deferred (OQ3/OQ4). Receipt: 2026-06-12T1830Z.silence-batch-close-audit.json"
  ],
  "hardening_done": [
    "verification-discipline hardening (77302ed): exit-codes-are-the-verdict rule in loop-protocol \u00a7 REVIEW + audit checklist; promoted to ~/.claude/CLAUDE.md \u00a7 Verification discipline; repo grep CLEAN (CI/scripts already bare/pipefail \u2014 masking was loop discipline, not repo); !metric_sets guard classified as 058-repair side-effect. Closed with bare `cargo test --all` exit 0, 1366 tests, 0 failures across 31 targets.",
    "PUSHED 2026-06-12 (restrictions lifted, weekend): nq dc78b7d..77302ed -> main; nq-blackbox 5948531..dcb8e66 -> main. origin now synced incl. prior nq-claude unpushed work."
  ],
  "completed_slices": [
    "inherited-slice-admission (A2): UI slice admitted 0f35a2c + path-fixups 834049c, mangled WAL doc reverted. Receipt: 2026-06-12T1615Z.inherited-slice-admission.json",
    "lane-a-nq-blackbox (A1): PARKED \u2014 probe promotion infra-gated (no exporter/nq-serve) AND a real upstream finding (scrape-target provenance is wire-only, not a queryable column). Receipt: 2026-06-12T1625Z.lane-a-blackbox-parked.json",
    "lane-b-closure-floorboards (B): NQ-CLOSE-003 shipped (docs/architecture/HOST_TRUST_BOUNDARY.md); NQ-CLOSE-002 policy locked (docs/working/decisions/NQ_RETENTION_WINDOWS.md). Receipt: 2026-06-12T1635Z.lane-b-floorboards.json",
    "lane-c-promotion-analysis: analysis-only, promoted nothing (docs/working/decisions/NQ_LANE_C_PROMOTION_ANALYSIS.md). Receipt: 2026-06-12T1645Z.batch-close-audit.json",
    "detector-taxonomy-silence-subtaxonomy (2026-06-15, cold context-switch from AG): SILENCE_UNIFICATION V1 step 4 \u2014 DETECTOR_TAXONOMY bucket 2 grew \u00a72a (silence contract fields + 3 mechanism shapes + shipped-vs-deferred detector status + witness-as-parent-evidence); flipped bucket-2 table row. Docs-only, commit 3b7c0da, doc-code reconciliation under standing auth. HEAD re-verified green on cold entry (cargo test --workspace exit 0). Receipt: 2026-06-15T1612Z.detector-taxonomy-silence-subtaxonomy.json",
    "probe-hygiene-exit-masking (2026-06-15): re_entry_probes[3] no longer masks the verifier exit \u2014 set -o pipefail + surfaced EXIT=${PIPESTATUS[0]}, tail kept for glance. Proven (synthetic exit 3 surfaces; real subset EXIT=0). Commit c5839ed. Receipt: 2026-06-15T1619Z.probe-hygiene-exit-masking.json",
    "witness-position-export-projection-gap (2026-06-15, operator-authorized doc-only): read-only NQ<->Nightshift topology sweep found WitnessPosition produced on nq.witness.v1/preflight-supports + Nightshift fully wired to render it (no-inference sentinel), but nq findings export does not project it onto nq.finding_snapshot.v1 -> consumer starved. Filed docs/working/gaps/WITNESS_POSITION_EXPORT_PROJECTION_GAP.md (status candidate / REFERRED to operator policy): closing it is a multi-witness attribution policy decision (scalar=badge/weak->strong laundering risk), not a wiring fix. Names shapes A/B/C, selects none; records operator's non-binding anti-scalar lean. No projection/schema/export/Nightshift edit. Receipt: 2026-06-15T1631Z.witness-position-export-projection-gap.json",
    "sql-contract-column-stability (2026-06-18, Lane A completeness; AG-Claude-as-actuator, AG Classic frozen): extended crates/nq-db/tests/sql_contract.rs from public-view existence to column existence (PUBLIC_VIEW_COLUMNS, 12 views, baselined from migrated schema; missing=fail, extra=allowed). Sibling receipt nq.sql_contract.public_columns.v1 (additive; existing public_views.v1 + NQ-on-NQ-002 consumer untouched). Doc: sql-contract.md 'What is mechanically enforced'. Additive-only diff; both verdicts PASS (cargo test -p nq-db --test sql_contract 3/3, full --all --locked exit 0). Commit 24ed6cb (no push). Receipt: 2026-06-18T1825Z.sql-contract-column-stability.json",
    "sql-contract-column-ordering (2026-06-18, Lane A impl / OPERATOR-DIRECTED guarantee strengthening): added append-only projection-order stability to the public-view column contract (check_column_order + OrderViolation{Misplaced,Truncated}; test asserts existence AND order across 12 views; negative unit test). Receipt public_columns.v1 gains per-view order fields; scope checks += ordering, does_not_check -= ordering. Doc updated. Honesty: adds a guarantee column order was previously unpromised \u2014 recorded in commit body + receipt, NOT laundered as routine completeness. Both verdicts PASS (4/4 + full --all --locked exit 0). Commit 47cab5c (no push). Receipt: 2026-06-18T1837Z.sql-contract-column-ordering.json",
    "freshness-horizon-repackaging-invariant (2026-06-18, C2/M1, Lane A completeness): one test in crates/nq-core/src/preflight.rs pinning that the freshness horizon is byte-identical under generated_at repackaging (two PreflightResults, different generated_at, same observed_at_max). Names the laundering shape: fresh wrapper != fresh evidence. Mechanizes an existing promise (VERDICTS.md:46-50 + CLAIM_KIND_DISK_STATE_GAP.md:188); +63/-0 one file. Both verdicts PASS. Commit 1ccf659 (no push). Receipt: 2026-06-18T1906Z.freshness-horizon-repackaging-invariant.json",
    "state-schema-reconciliation (2026-06-18, OPERATOR-DIRECTED contract reconciliation): GET /state now carries the documented nq.witness_packet.v1 envelope schema (additive; absent->None, never laundered to v1). Verified green (nq-core+nq-witness exit 0; full --all --locked exit 0). Commit e8b3c94. Receipt: 2026-06-18T1933Z.state-schema-reconciliation.json. (Was review_pending_commit; committed since.)",
    "active-witness-tls-probe 2a-2d-a (2026-06-19, operator-directed; microslice+repair): first complete active witness \u2014 external TLS-cert probe. 2a clock-injected verdict core nq.probe.tls_cert.v1 (e8d3f34); 2b live observe-only transport (0599b5a); 2c WebPKI chain validation at the probe clock (f87ee68); 2d-a manual append-only receipt series, no scheduler (b5d35ec). Receipts: 2026-06-19T{2132,2256,2331,2356}Z.tls-cert-*.json. Frozen as a specimen 2026-06-23.",
    "nq-render-witness-boundaries (2026-06-23, Lane A render/copy of MONITORING_PROJECTION_SEAM): witness-report frame (refuses incident-commander + proof-checker readings) + per-finding scan-surface cannot-testify boundary + source_quiet collector-scoped wording + age persistence-not-neglect + README 'What NQ is not'. No Lane B. Commit ccc9a84. Pinned by crates/nq-monitor/tests/render_claim_boundaries.rs. cargo test -p nq-monitor exit 0.",
    "nq-canon-carriage (2026-06-23, Packet 1, render/copy): carry recorded warning canon (work_state/owner/note/external_ref) from v_warnings into the overview WarningVm + a terse Canon: scan-row line, verbatim + HTML-escaped, no synthesis, no authority promotion (acknowledged/work_state never render as healthy). No migration (columns pre-existed in the view). Commit 96cb391. cargo test -p nq-monitor -p nq-db exit 0.",
    "tls-specimen-freeze (2026-06-23, Packet 2, docs + receipt sanity only): docs/specimens/first_complete_active_witness_tls.md written from verified artifacts. Sanity PASS \u2014 commit order correct, 4 governor receipts persisted, manual runs/ capture present + gitignored, stdout preserved, validation uses the injected probe clock (not ambient now). Commit ace0d35. Receipt: 2026-06-23T1720Z.tls-specimen-freeze.json.",
    "pfsense-reachable-drift declared-deny custody check #1 (2026-06-24, gold finding, blocked-only landed): commit c65bc98 (core declared_deny_probe.rs + transport declared_deny_transport.rs + CLI + doc). Declaration-vs-observation CUSTODY test, NOT firewall-correctness. Source typing: loaded block rule = pfSenseDeclaration; counters = pfSenseRuntimeReport (never an independent observation); NQ probe = ObservedReachability. Asymmetry: only a SUBJECT got-through (real handshake) is the unambiguous contradiction; a blocked subject is admissible only with a passing CONTROL (egress proof); a block-return RST is not a got-through; absent rule = cannot_testify, never 'allowed'. Ladder: unknown_custody_policy_surface / cannot_testify_declared_policy_absent / _probe_target_unbound / _vantage_unbound / declared_deny_probe_inconclusive / declared_deny_observed_reachable (contradiction) / declared_deny_observed_blocked (corroborated). Read-only discovery: WAN igc0 + LAN igc1 only (no guest VLAN; sushi-k-lan is the only vantage); NO IDS/IPS installed (low perturbation); found USER_RULE: pfB_PRI1_v4 (block return quick on igc1 to <pfB_PRI1_v4>, 17007 entries, 3.19M evals/8 blocked/0 states). LIVE blocked-only read from sushi-k-lan: rule present+custodied, control 1.1.1.1:443 reached (egress proven), subject DELIBERATELY UNBOUND -> cannot_testify_probe_target_unbound. OPERATOR-DIRECTED: do NOT SYN a malware-blocklist member to make the specimen spicy; active subject probe PARKED for a scratch/lab firewall + controlled deny rule + benign target (e.g. 8.8.8.8) + captured teardown. Receipts gitignored under runs/declared-deny/ (only rule declaration/table-name/counters, never member IPs; fixtures TEST-NET). cargo test -p nq-monitor EXIT=0, 0 warnings. Receipt: 2026-06-24T1735Z.declared-deny-custody.json.",
    "three-host-deploy-bake-verify (2026-06-24, deployment completeness): the overnight bake of commit 6a8c443 verified green on all three hosts \u2014 NRestarts=0 since each deploy restart (sushi-k 21:54Z / NAS 22:02Z / VM 22:10Z), HTTP 200 everywhere, VM journal -p warning last 12h = no entries, public nq.neutral.zone 200 + zero leak language. Read-only checks (systemctl is-active/show, curl 127.0.0.1:9848, journalctl), no host mutation. Recorded in 2026-06-23T2211Z.three-host-deploy.json -> bake_verified. No new push (6a8c443 already on origin/main).",
    "pfsense-reachable-drift gateway-path Phase 1+2 (2026-06-24, CLOSED): check #3 gateway-report-vs-path. Phase 1 pure core (6fe3438) then OPERATOR-DIRECTED witness-custody revision + Phase 2 live read (80295fb). Custody revision: raw dpinger socket is the FIRST-CLASS witness, NOT pfSense's UI classification (return_gateways_status/PHP = second-order projection, deferred as a later comparator); core rewritten from classified GatewayStatus to a dpinger-metrics-first custody model (socket_absent/_unreadable/unknown_custody are first-class cannot_testify; only loss<100 derived from raw metrics). Ladder: cannot_testify_dpinger_socket_absent/_unreadable/unknown_custody, cannot_testify_no_path_basis, corroborated_by_path, path_ambiguous (specimen), egress_trouble_not_wan_down. Transport (gateway_path_transport.rs): tested parsers for socket filename (identity) + status line; one read-only SSH gather (ls + nc -U the dpinger socket, NO PHP/pfctl/service-control); ICMP-first probes (monitor IP + anchor 1.1.1.1 as separate witnesses), TCP fallback only when ICMP can't execute; append-only sink. CLI `nq-monitor probe gateway-path` (read-only, receipt-only). LIVE vs real box (pfSense 2.8.1, one WAN_DHCP gw over CGNAT) from sushi-k-lan: observed corroborated_by_path (dpinger loss 0% + both ICMP reached) AND cannot_testify_dpinger_socket_absent (non-existent gw stays cannot_testify even with 1.1.1.1 reachable). Receipts gitignored under runs/gateway-path/; real WAN/monitor IPs NOT committed (fixtures TEST-NET 198.51.100/24; paranoia scan clean). cargo test -p nq-monitor EXIT=0, 0 warnings. Receipt: 2026-06-24T1640Z.gateway-path-live-read.json. PARKED next: PHP comparator receipt (pfSense reports status X \u2014 the deferred second-order witness); external/off-LAN vantage; Kea; check #1 declared-deny (gold finding, own packet).",
    "pfsense-reachable-drift lease-presence Phase 1+2 (2026-06-23, CLOSED): Phase 1 \u2014 inventory/source-typing doc + pure lease-presence core nq.probe.lease_presence.v1 (commit 3599c85). Phase 2 \u2014 live read: one read-only SSH login (ISC dhcpd backend detected; cat lease file + arp -an) + optional ICMP from the LAN vantage, wired to the core (commit 68f9c61). Observed corroborated AND the non-lift uncorroborated (lease_uncorroborated does NOT mean host down/gone/lease-wrong) against the real box. Live receipts are gitignored under runs/; no host/MAC/private-key data committed (parser fixtures anonymized; paranoia scan scrubbed a real MAC/IP from new files; the STEP0 inventory doc retains intentional LAN-topology grounding the operator authored). Full cargo test -p nq-monitor -p nq-db EXIT=0. PARKED next: gateway-report-vs-path (#3, cheap, same family); declared-deny enforcement (#1, gold finding \u2014 needs a guest-VLAN vantage + perturbation budget, its own packet); Kea lease parsing when a box uses it. Receipt: 2026-06-23T1925Z.pfsense-lease-presence-live-read.json."
  ],
  "next_action": "*** DAYLIGHT DEPLOY 2026-07-03 (holiday, operator-cleared) \u2014 ChatGPT order steps 1-4 DONE. (1-3) THREE-HOST FLEET DOCKER DEPLOY: sushi-k canary -> NAS -> VM, schema 60->63 (commit 38d0167; migrations 061 tombstones/062 retirement/063 zfs-history), per-host AGGREGATOR-FIRST restart (nq-serve before nq-publish). Docker native capture LIVE in prod: sushi-k governor-code-adapter running+healthy; VM driftwatch running+healthy, caddy+pds running+NULL (no-healthcheck path). All GREEN: dash 200, PUBLIC nq.neutral.zone 200, boundaries present, leak 0. Receipt 2026-07-03T1622Z.three-host-deploy-schema63-docker-native.json. (4) BLACKBOX BUCKET 1 PROVE-THE-WHEEL: blackbox_exporter v0.28.0 live on sushi-k (user-systemd, promoted nq_aggregator_http_localhost http_2xx) + VM (system-systemd, promoted external_http_nq_neutralzone https public-path; valid LE cert observed). Both smoke_probe.sh SMOKE PASS, collision=0. nq-blackbox @ e768f63 PUSHED (unpingable/nq-blackbox). Receipt 2026-07-03T1642Z.blackbox-bucket1-deploy-prove-the-wheel.json. LIVE FINDING: blackbox /probe emits BARE probe_success -> NQ series identity (metric_name,labels_json) collapses 2 targets/host to collision=1, so ONE clean bare-probe_success target per island. PARKED next levers (operator to choose, un-opened): (a) scrape-target IDENTITY migration NQ_SCRAPE_TARGET_IDENTITY_SCOPE = unblocks multi-probe-per-host blackbox (code slice, not deploy); (b) ChatGPT order STEP 5 paper layer = WITNESS_CLAIM_SCOPE / C2 dual-marker follow-ons / basis-stale audit (new code surfaces, deferred until deploy receipts exist \u2014 they now do). Loop AUDIT/idle, parked per operator 2026-07-03. *** \n--- PRIOR: *** DEPLOYED 2026-06-29 ~6pm to ALL THREE running instances (operator go, after-5pm). commit 72cb055 / schema 58->59 (service_state witness family). sushi-k canary -> NAS (lil-nas-x) -> VM (nq.neutral.zone), every host GREEN: dash 200, migration 059 clean, render boundaries present, leak language 0; PUBLIC nq.neutral.zone 200. PRODUCTION-VERIFIED LIVE: sushi-k service_observations has 35 real rows (cron/smartmontools/ttyd/rsyslog/... native active/running) \u2014 the live systemd collector works in prod, not ornamental. Per-host pre-servicestate rollback (binaries + pre-migration DB) retained, not needed. NAS scp hit ETXTBSY -> scp-to-.new+mv; VM stop-before-swap. project_deployment memory + receipt 2026-06-29T2215Z.three-host-deploy.json updated. *** PUSHED 2026-06-29 ~6pm (operator green light; cadence = after-5pm-or-weekend, NOT weekend-only). origin/main == HEAD for BOTH repos (0 ahead): unpingable/nq ed02499..a632be6 (23 commits), unpingable/nq-witness 6c6fe15..44909b0 (3 commits). Whole session arc now public. Receipt 2026-06-29T1755Z.session-push.json. Loop AUDIT/idle. DEFERRED (named, not started): service_state feed projection into PreflightSupport.witness_packet + served_surface_registry + docker/process native capture; Kea live-wire; P1 adapters. --- PRIOR (now pushed): SERVICE_STATE LIVE COLLECTOR (systemd) + witness projection \u2014 DONE 2026-06-29 (was LOCAL, NOT pushed). Loop AUDIT/idle, STOP per operator sequence. service_state now testifies from LIVE observations: services.rs check_systemd captures native ActiveState/SubState/LoadState/UnitFileState (one systemctl show); ServiceData+ServiceRow +4 serde-default fields (wire-additive; pull/mod + e2e carry them; ~14 ServiceRow sites updated); publish.rs 5c writes service_observations for systemd rows each cycle (active NOT promoted to healthy); service_state_witness_projection -> nq.witness.v1 (proof-of-shape, plain coverage_limits, no claim vocab, wire-validated); manifest +2 backends. Tests: live_systemd_collection end-to-end (collect->publish->evaluator admissible_with_scope, refusals intact) + 3 projection; full suite green. SCOPE: systemd ONLY (docker still coarse-status); no Kea live-wire, no P1. DEFERRED (named): feed projection into PreflightSupport.witness_packet (still None); served_surface_registry; docker/process native capture. FEATURE_HISTORY \u00a7 SERVICE_STATE_LIVE_COLLECTOR; receipt 2026-06-29T1810Z.service-state-live-collector.json. PUSH: nq ~24 ahead, HELD workweek (operator: nearing 5pm, weekend push soon). --- PRIOR: SERVICE_STATE V0 (schema slice, the deferred #1 \u2014 OPENED by operator) \u2014 CORE DONE 2026-06-29 (committed LOCAL, NOT pushed \u2014 workweek no-push). Loop AUDIT/idle. Opened the deferred service_state slice; breadcrumb ratified the minimal shape; ChatGPT confirmed columns. LANDED: migration 059 service_observations (native active/sub/load/unit_file_state + host/manager/service identity + UNIQUE index; NO recovered/healthy/safe/coverage cols; CURRENT_SCHEMA_VERSION 58->59); ServiceState ClaimKind + PREFLIGHT_SERVICE_STATE_SCHEMA + service_state_cannot_testify; nq-db service_state module = writer insert_service_observation (idempotent same-state, EXPLICIT conflict on differing state, no silent overwrite) + reader latest_service_observation_for_tuple + evaluator evaluate_service_state_preflight* (missing->insufficient_coverage 'no witness not false'; fresh->admissible_with_scope witness-scope-only; stale->stale_testimony); nq_evaluator_probe dispatch arm; coverage manifest service_state deferred->implemented; breadcrumb->partial. THREE-LAYER shape held (per-kind table -> projection[DEFERRED] -> receipt). 6 module tests + 2 migration CHECK/UNIQUE green; full suite (verify at commit) \u2014 ClaimKind ripple clean. WLP stayed out. FEATURE_HISTORY \u00a7 SERVICE_STATE_V0; receipt 2026-06-29T1730Z.service-state-v0.json. DEFERRED (named): service_state_witness_projection -> nq.witness.v1 (PreflightSupport.witness_packet None until then); LIVE COLLECTOR WIRING (capture native systemd/docker state into service_observations during a real cycle; collector today emits coarse ServiceStatus findings); served_surface_registry entry; docker/process variants. PUSH: nq ~20 ahead, nq-witness 3, HELD workweek. P0 FINAL: #1 service_state core DONE (was deferred, operator opened it); #2 DONE; #3 DONE. --- PRIOR P0 #2 EXPECTED_COVERAGE MANIFEST DONE 2026-06-29 (LOCAL, NOT pushed). Loop AUDIT/idle. *** All three P0 items resolved: #1 service_state DEFERRED (schema slice; breadcrumb preflights/SERVICE_STATE.md filed \u2014 three-layer shape + refusal boundary pinned, storage columns candidate; WLP noted as adjacent courier layer not NQ schema); #2 DONE; #3 DONE. *** #2: coverage/manifest (machine-readable: category|name|status|evidence|rationale; 8 claim kinds + 5 active probes + 4 lab-backed backends + service_state deferred) + scripts/check-coverage-manifest.sh (CI job coverage-manifest, fail-closed) + coverage/README. Fails closed on: implemented-surface-missing (ClaimKind variants / *_probe.rs / fixtures/<dir>), dead evidence, deferred-without-rationale. All 3 modes PROVEN. Scope = NQ-repo surfaces only (nq-witness profiles are that repo's concern; nq CI has no sibling checkout). Distinct from runtime substrate-coverage (SUBSTRATE_COVERAGE_DECLARATION_GAP, still candidate). FEATURE_HISTORY \u00a7 EXPECTED_COVERAGE_MANIFEST; rollout expected_coverage bucket; receipt 2026-06-29T1640Z.expected-coverage-manifest.json. CI now 5 jobs: test, gap-status, receipt-reattestation, witness-boundaries, coverage-manifest. PUSH: nq ~20 ahead, nq-witness 3 ahead, HELD workweek. REMAINING (operator-gated): live-wire Kea control-socket (gated real Kea integration via nc -U over SSH); the deferred service_state schema slice. --- PRIOR P0 #3 KEA CONTROL-SOCKET BACKEND DONE 2026-06-29 (LOCAL, NOT pushed). Loop AUDIT/idle. *** P0 #1 service_state DEFERRED (operator+ChatGPT confirmed NOT implementation-ready): CLAIM_CATALOG says witness shape undecided; no ServiceState ClaimKind / observation storage / preflight doc. NOT built; no migration invented by dns_state pattern-matching. Returns as a dedicated schema/design slice. *** #3: crates/nq-monitor/src/kea_control.rs \u2014 parse_lease4_get_all (pure) + fetch_leases_via_control_socket (unix socket), producing the EXISTING KeaLease shape via lease4-get-all (expire=cltt+valid-lft), no new abstraction. Typed KeaControlError per boundary (SocketMissing/ConnectionRefused/Timeout/Io/MalformedResponse/UnsupportedCommand=result2/KeaResult). Real captured fixture tests/fixtures/kea/lease4_get_all.json. 11 tests + 1 gated (#[ignore] real Kea via NQ_KEA_CTRL_SOCKET): parser+CROSS-BACKEND-CONSISTENCY(API==memfile)+result-codes+fake-unix-socket(happy/missing/malformed/timeout/refused). 0 warnings. NOT wired into live_lease_presence (control-socket-over-SSH via nc -U = gated real-integration step). FEATURE_HISTORY \u00a7 KEA_CONTROL_SOCKET_BACKEND; rollout kea bucket + nq-witness profile note; receipt 2026-06-29T1600Z.kea-control-socket-backend.json. PUSH: nq ~15 ahead, nq-witness 3 ahead, HELD workweek. NEXT: P0 #2 expected_coverage manifest \u2014 SCOPED to existing/ready surfaces only; MUST represent service_state as explicitly deferred/not-expected (pointer to CLAIM_CATALOG), NOT force it into existence; machine-readable + CI-checked; prevents silent absence (no 'absence means okay'). --- PRIOR DECISION D SOURCE-CONFORMANCE DOCTRINE DOC DONE 2026-06-29 (LOCAL, NOT pushed). Loop AUDIT/idle. Wrote docs/working/decisions/BASELINE_SOURCE_CONFORMANCE.md (candidate/non-authorizing): tiers (Tier1 conforming witness / Tier2 observation source / Tier3 generic metrics / Prom-projection-of-witness), promotion rule, native-adapter forcing-case, shared-source-contamination!=corroboration, + per-exporter matrix (node/postgres/redis/mysql/nginx/blackbox/cadvisor/process/telegraf with what-each-cannot-carry + promotion path). Pointer added from integrations.md (stays setup); rollout Decision D resolved + Sources lane points to it. Grounded vs nq-witness SPEC + RELATIONSHIP_TO_PROMETHEUS. Doc-only, no code. receipt 2026-06-29T1510Z.source-conformance-doc.json. *** ALL FOUR rollout decisions A/B/C/D now CLOSED; baseline-rollout ontology fully front-loaded. *** PUSH: nq ~11 ahead, nq-witness 1 ahead, HELD workweek. NEXT per rollout P0: DNS done, TLS done, Kea profile done, time_basis sanity done \u2014 remaining P0 implementation = service_state generic claim, expected_coverage manifest, Kea control-socket backend; or pause. Operator direction. --- PRIOR TIME-BASIS INTERNAL SANITY V0 (Decision C) DONE 2026-06-29 (LOCAL, NOT pushed). Loop AUDIT/idle. CAUGHT a near-duplicate: check 1 (future-skew) ALREADY wired in preflight.compute_time_basis (observed_at_future_of_evaluator -> Suspect); my first draft duplicated it + collided on TimeBasisAnnotation. Reframed to the genuinely-missing check 2: crates/nq-core/src/time_basis.rs::observed_at_regression \u2014 pure detector for witness_observed_at stepping sharply BACKWARD per host/stream across cycles, tolerates jitter, suspicion_kind observed_at_backward_regression. ANNOTATION-ONLY/INERT (no verdict/refusal/correction/mutation; pending a follow-on feeding prior-cycle observed_at at ingest). 12 tests (4 new + 8 existing) green; 0 warnings. TIME_BASIS_POISONING_GAP V0 note + FEATURE_HISTORY \u00a7 TIME_BASIS_SANITY_V0 + rollout time_basis bucket + Decision C resolved; receipt 2026-06-29T1440Z.time-basis-sanity-v0.json. DEFERRED: wire check 2 at ingest; the other 4 checks; claim-layer consumption (refuse/downgrade, vocab ratify first); external clock_skew witness. DECISIONS: A/B/C all DONE; D (prom-conformance doc) remaining. PUSH: nq ~10 ahead, nq-witness 1 ahead, HELD workweek. NEXT: Decision D (BASELINE_SOURCE_CONFORMANCE / prom curation doc + integrations pointer) OR pause. --- PRIOR TLS CERT VERDICT-LADDER LAB VALIDATION DONE 2026-06-29 (LOCAL, NOT pushed). Loop AUDIT/idle. TLS was the MOST-built of the three (nq.probe.tls_cert.v1: core+transport+WebPKI+series+CLI+refusals all shipped 2026-06-19); parse_presented_cert already had a real single-SAN leaf test. GAP: the verdict ladder (evaluate_tls_cert) had only met SYNTHETIC facts. Generated a controlled multi-SAN self-signed cert (OpenSSL 3.0, SAN tls-lab.test+www.tls-lab.test, validity 2026-06-29..2027-06-29) -> tests/fixtures/tls/lab_leaf.pem + README; 6 tests drive the ladder from the REAL parsed cert via INJECTED CLOCK (ValidAtProbeTime / second-SAN / ValidButWithinWarningHorizon / ExpiredUnderProbeClock / NameMismatch + multi-SAN parse). LANE DECIDED/recorded: nq.probe.tls_cert.v1 is an ACTIVE-WITNESS PROBE (receipt-only, clock-injected), sibling of gateway-path/lease-presence/declared-deny, NOT a preflight claim kind ('tls_certificate_state preflight' was a mislabel; corrected in rollout doc). Refusals pinned in scope_ceiling_non_claims, now exercised vs real cert. 0 warnings; gates green. FEATURE_HISTORY \u00a7 TLS_CERT_VERDICT_LAB_VALIDATION; rollout TLS bucket updated; receipt 2026-06-29T1410Z.tls-cert-verdict-lab-validation.json. DEFERRED: 2d-b operationalization (scheduler); WebPKI-untrusted real-chain fixture for chain_invalid. DECISIONS: A done; B done; C time_basis sanity NEXT (per 'both, TLS first'); D prom-conformance doc pending. PUSH: nq ~8 ahead, nq-witness 1 ahead, HELD workweek. NEXT: time_basis internal sanity annotation (Decision C \u2014 2 checks annotation-only: future observed_at, per-stream regression). --- PRIOR KEA FULL WITNESS PROFILE (Decision B) DONE 2026-06-29 (LOCAL both repos, NOT pushed). Loop AUDIT/idle. Wrote nq-witness/profiles/kea_dhcp.md (nq.witness.kea_dhcp.v0) grounded in the real kea-dhcp4 2.2.0 lab data; SPEC profiles index updated. Required coverage daemon_state/lease_identity/lease_lifecycle/subnet_pool_identity; observation kinds kea_daemon/kea_lease/kea_subnet/kea_reservation; key semantics lease_state(default/declined/expired-reclaimed) INDEPENDENT of expire_at, fqdn_fwd/rev = DDNS INTENT not DNS-content; inadmissible_for = connectivity/identity-ownership/authorization/reachability/dns-correctness/reverse-dns/renewal/network-health/remediation/failover. Native parse_kea_leases (memfile, lab-backed) recorded as FIRST backend adapter with HONEST partial coverage (everything beyond lease identity/lifecycle demoted to cannot_testify). Future composite dhcp_dns_identity_consistency NAMED not built. nq-side: BASELINE_SERVICE_ROLLOUT kea bucket + Decision B resolved; FEATURE_HISTORY follow-on note; receipt 2026-06-29T1340Z.kea-witness-profile.json. SCOPE: full PROFILE now, not full adapter maximalism \u2014 did NOT build control-socket backend / kea_dhcp_state claim kind / composite. PUSH: nq 6 commits ahead, nq-witness 1 ahead, BOTH HELD workweek. DECISIONS: A done (dns); B done (kea profile); C time_basis internal sanity (2 checks annotation-only) PENDING; D prom-conformance doc PENDING. NEXT per rollout: TLS certificate-state spec (refusals first) OR time_basis sanity (Decision C). --- PRIOR DNS V0 CLOSEOUT DONE 2026-06-29 (LOCAL, NOT pushed). Loop AUDIT/idle. Returned to BIND 9.18 docker lab (added reverse zone for PTR, TXT record, forward-to-dead-upstream for a REAL servfail); captured servfail/ptr/txt bytes. response_kind matrix now ALL FIVE RCODE-bearing kinds real-validated (success/nodata/nxdomain/servfail-NEW/refused); PTR+TXT answer decoding validated (PTR = separate testimony, not inferred reverse). 7 real-bytes parse_response tests (removed 2 crafted dups \u2014 existing synthetic suite already covers truncation/16-RCODE/compression/fuzz). DNS_WITNESS_FAMILY_GAP V0 closeout section (matrix, qtypes A/AAAA/NS/CNAME/SOA/PTR/MX/TXT/SRV, refusal boundaries: no health/reachability/global-truth/authoritative/DNSSEC/recursive-vs-authoritative/consequence, no TCP/EDNS) + Decision A (dns_state BOTH: native now, witness-JSON nq.witness.dns_state.v0 normalizable near-term, NOT blocked). FEATURE_HISTORY updated; receipt 2026-06-29T1300Z.dns-v0-closeout.json. timeout/transport_error = socket-layer (synthetic-covered); validation_failure reserved (DNSSEC). 0 warnings; gates green; full suite (verify at commit). DECISIONS RESOLVED (operator+ChatGPT, for FUTURE slices): A dns both; B Kea FULL witness profile now (keep native reader as backend); C time_basis internal sanity annotation now (2 checks: future observed_at, per-stream regression; annotation-only, no verdict/refusal/consequence); D new Prom-conformance doc + integrations.md pointer. PUSH: 4 commits ahead of origin, HOLDING workweek. NEXT per rollout: TLS certificate-state spec (refusals first) OR Kea witness profile OR time_basis sanity. --- PRIOR BASELINE SERVICE ROLLOUT MATRIX (planning doc) DONE 2026-06-29 (LOCAL, NOT pushed). Loop at AUDIT/idle. Operator+ChatGPT direction: baseline TESTIMONY FAMILIES not services; build the matrix BEFORE more adapters (anti-sprawl). Wrote docs/working/decisions/BASELINE_SERVICE_ROLLOUT.md (candidate/non-authorizing). Load-bearing reframe VERIFIED vs nq-witness SPEC (cloned at ~/git/nq-root/nq-witness): observation source (visibility) != conforming witness (standing); Prom exporters are observation sources by default, feed GENERIC detectors only, need wrapping into canonical witness JSON before profile-specific standing; Prom projection of a witness = optional convenience not source-of-truth. Buckets P0: dns_state (existing, closeout next), tls_certificate_state (partially built, next protocol witness), service_state (named-but-not-built generic claim), expected_coverage, time_basis (proposed, foundational \u2014 witness reports offset, claim layer decides standing, witness must NOT conclude freshness-invalid), kea harden+profile, smart/zfs/fs_inode profile harden. Verified facts: dns qtypes A/AAAA/NS/CNAME/SOA/PTR/MX/TXT/SRV; service_state named-but-not-built (no recovery witness); TIME_BASIS_POISONING_GAP proposed two-layer. Open decisions A-D (dns shape native-vs-witness-json; kea profile timing; time_basis first-code timing; prom curation surface). NO adapter code (stopped after the doc per ChatGPT). RECOMMENDED NEXT SLICE: DNS closeout (response_kind fixture matrix incl servfail/timeout/transport_error + qtype/refusal example docs; PTR separate testimony; no TCP/DNSSEC/health), then TLS cert-state spec (refusals first). PUSH STATUS: now 3 commits ahead of origin (2 DNS + this doc); HOLDING workweek no-push. --- PRIOR DNS WIRE-DECODER LAB VALIDATION DONE 2026-06-29 (committed LOCAL, NOT pushed \u2014 workweek no-push). Loop back to AUDIT/idle. DNS V0 (dns_state) was already mostly shipped (ResponseKind, evaluator nq-db/dns.rs, UdpDnsClient, probe dns CLI); the RISKY uncovered part was the hand-rolled UDP wire decoder probe.rs::parse_response (previously only synthetic-tested via outcome_from_wire, never met a real resolver). Spun up REAL BIND 9.18.49 in docker (authoritative lab.test, recursion off), captured raw response datagrams for success/nodata/nxdomain/refused by querying an in-container python UDP client (docker fs sharing broken in this rootless daemon \u2014 mounts+build-context fail; only stdin+container-net work, so did config+capture in-container; --rm, no residue). Committed crates/nq-monitor/tests/fixtures/dns/*.hex (real bytes) + README (compatibility-not-live + reproduce); 5 probe::tests feed real bytes to parse_response (success decodes A+ttl; nodata is NOT success \u2014 load-bearing split; nxdomain; refused; id-mismatch->TransportError). Decoder now KNOWN-correct vs real wire (no bug surfaced). 129 probe tests green; 0 warnings; floor gates green; full suite green. FEATURE_HISTORY \u00a7 DNS_WIRE_DECODER_LAB_VALIDATION; DNS_WITNESS_FAMILY_GAP lab-validation note (still proposed for family expansion); receipt 2026-06-29T1210Z.dns-wire-decoder-lab-validation.json. LABELED lab-backed COMPATIBILITY evidence, not live testimony. DEFERRED: servfail real capture (RCODE2, shares decode path); DNSSEC validation_failure; TCP-fallback/EDNS; DNS family V0 expansion. PUSH STATUS: 2 commits ahead of origin after this + prior Kea (ed02499 was last pushed); HOLDING per workweek no-push. NEXT: more zoo (STORAGE_BACKEND / INSTANCE_WITNESS / AGENTIC_CI families) or pause \u2014 operator direction. --- PRIOR KEA LEASE ADAPTER DONE 2026-06-29. Loop back to AUDIT/idle. CONTEXT: operator escalated the YAGNI misapplication ('YAGNI until I run it' is the WRONG law for NQ); promoted corrected doctrine to global CLAUDE.md (Recognition vs authority) + NQ memory feedback-yagni-adapter-vs-live-authority. ADAPTER built lab-backed: parse_kea_leases/kea_lease_state/kea_lease_report_for in lease_presence_transport.rs (sibling to ISC reader; backend-agnostic core unchanged); live_lease_presence routes DhcpBackend::{Isc,Kea}. Format CAPTURED from a real kea-dhcp4 2.2.0 docker lab (lease_cmds hook, lease4-add via control socket) \u2014 NOT invented. Learned: memfile cols address,hwaddr,client_id,valid_lifetime,expire(abs unix),subnet_id,fqdn_fwd,fqdn_rev,hostname,state(0 default/1 declined/2 reclaimed),user_context; state col != validity (state-0 can be lapsed by expire); lease4-* need the lease_cmds hook. Committed lab fixture crates/nq-monitor/tests/fixtures/kea/ (real CSV + README labeling compatibility-not-live + reproduce). 9 unit tests (real fixture + negatives) green; 0 warnings; full suite green; floor gates green. Receipts/fixture LABELED lab-backed COMPATIBILITY evidence (collector observes a Kea surface under declared conditions), NOT live-estate testimony \u2014 the cage label is the anti-laundering guard. FEATURE_HISTORY \u00a7 KEA_LEASE_ADAPTER; PFSENSE_REACHABLE_DRIFT_STEP0_INVENTORY updated; receipt 2026-06-29T1145Z.kea-lease-adapter.json. DEFERRED: Kea control-socket API reader (lease4-get-all JSON captured); live Kea-box read (=live testimony); other filed witness-family adapters (DNS_WITNESS_FAMILY, STORAGE_BACKEND, INSTANCE_WITNESS, AGENTIC_CI_WITNESS_FAMILIES) for same lab-backed treatment. NEXT: more adapter coverage (build the zoo) OR #8 PHP comparator \u2014 operator direction. --- PRIOR Packet #7c GATEWAY-PATH EXTERNAL-ARRIVAL CORROBORATION DONE 2026-06-28 (folded #7b into gateway-path). Loop back to AUDIT/idle. ADDITIVE combiner in gateway_path_probe.rs: reads unchanged LAN-side GatewayPathVerdict + optional ExternalArrivalBasis (parsed from nq.beacon_status.v0) -> nq.probe.gateway_path_combined.v1 {lan_basis, external_basis, combined: corroborated/divergent/cannot_classify, cause_not_inferred:true}. Matrix: alive+arrival=corroborated; not_alive+absence=corroborated(neg concordance); alive+absence & not_alive+arrival=divergent; lan_unknown+any=cannot_classify (external NEVER overrides LAN-unknown \u2014 load-bearing); no-external/no-basis=cannot_classify. Wired: nq-monitor probe gateway-path --external-beacon-status <path>. 7 combiner unit tests (full matrix + parser) green; cargo build 0 warnings; existing gateway-path verdicts UNCHANGED. Doctrine: position diversity corroborates or diverges, never launders absence into cause. FEATURE_HISTORY entry; gap doc outcome section; receipt 2026-06-29T0120Z.gateway-path-external-corroboration-7c.json. SCOPE HELD: no perpetual beacon, absence!=WAN-down, external!>LAN, no #8/#9/#10. NEXT: reassess before #8 (pfSense PHP comparator = appliance self-description, clearly lower-rank now); #9 Kea (YAGNI until a box runs Kea); #10 TLS 2d-b; or stop on clean floor. D/E household chores remain. --- PRIOR Packet #7b EGRESS-LIVENESS EXTERNAL WITNESS MINIMAL SLICE DONE 2026-06-28. Loop back to AUDIT/idle. CGNAT-compatible shape: LAN host (sushi-k) beacons OUT over existing SSH; external vantage (public VM) witnesses arrival at its own clock. scripts/beacon/{beacon-receive,beacon-emit,beacon-status}.sh + README + reference systemd units (NOT enabled). Live-verified end-to-end: no-basis -> emit -> arrival_witnessed (age 1s) -> controlled absence_at_vantage (threshold 0, with 'NOT wan_down by itself' note). Custody clean: VM log = {vantage-time,nonce,'nq-lan-egress'} only, no LAN data; SSH key stayed on LAN; public nq 200 unaffected; no caddy/nq-app change; CI unaffected (beacon = operational tooling, not a gate). Doctrine: a received beacon witnesses external arrival; a missing beacon witnesses absence-at-vantage NOT its cause (emitter/SSH/VM-load/route/WAN). FEATURE_HISTORY entry; gap doc EXTERNAL_GATEWAY_PATH_VANTAGE -> partial; receipt 2026-06-29T0045Z.egress-beacon-7b.json. DEFERRED (named): supervised low-rate cadence (timer committed not enabled = explicit operator decision); fold absence_at_vantage into gateway-path witness as corroborating position; dedicated clean external vantage if contamination confounds. NEXT: REASSESS before #8 (pfSense PHP comparator = appliance self-description, different/weaker epistemic class than external arrival), then #9 Kea, #10 TLS 2d-b. D/E household chores remain. --- PRIOR Packet #7 EXTERNAL/OFF-LAN GATEWAY-PATH VANTAGE SCOPING SLICE DONE 2026-06-28 (positioning, NOT probe-build). Loop back to AUDIT/idle. Selected vantage: public NQ VM (labelwatch/Linode) declared SHARED SUBSTRATE v0 (rejected Mac mini = on-LAN; deferred dedicated clean Linode = YAGNI). DECISIVE FINDING: home WAN behind CGNAT -> NO inbound reachability -> external vantage CANNOT directly probe home WAN gateway; viable channel is egress-liveness (home beacons OUT to VM, VM witnesses arrival/absence). Declared witness-position + perturbation budget (receive-only on VM, low-rate egress beacon, back off, stop conditions) + custody boundary (VM holds only benign receive-side timestamps; pfSense key/dpinger/runs/WAN-addr/LAN-MACs stay LAN-local). Read-only smoke test done (VM external vantage live, sushi-k->VM 200, no home probing, no LAN data left LAN). Artifact docs/working/gaps/EXTERNAL_GATEWAY_PATH_VANTAGE_GAP.md (proposed); receipt 2026-06-29T0015Z.external-vantage-scoping.json. Doctrine: external vantage is position diversity, not higher authority. NEXT: #7b implement egress-liveness witness (LAN beacon -> VM) using declared vantage+budget+custody, verdict semantics UNCHANGED \u2014 gated on operator direction. Then #8 pfSense PHP comparator, #9 Kea, #10 TLS 2d-b. --- PRIOR Packet #6 BUILD-GRAPH-ENFORCED WITNESS/PROBE BOUNDARY DONE 2026-06-28 (opened only after CI fully green on main). Loop back to AUDIT/idle. Structural enforcement that witness crates cannot name NQ's persistence/coercion surface: scripts/check-witness-boundaries.sh reads the resolved cargo dep graph and fails closed if nq-witness/nq-witness-api ever gain nq-db (a witness that could write nq-db could manufacture its own findings). Three fail-closed layers (forbidden checks + control tripwire nq-monitor->nq-db + self-test). Proven end-to-end: real injected nq-witness->nq-db edge -> gate exit 1; revert -> 0. CI job witness-boundaries; doctrine docs/working/decisions/WITNESS_PROBE_BOUNDARY.md + FEATURE_HISTORY entry; receipt 2026-06-28T2355Z.witness-probe-boundary.json. Allowed exceptions classified (read-only systemctl-show observation; witness scratch-sqlite WAL probe). NAMED DEFERRED STRUCTURAL GAP (not closed): active-witness probes (crates/nq-monitor/src/*_probe.rs) are intra-nq-monitor (depends on nq-db) so build graph can't wall them; structural fix = extract nq-probe crate (architecture refactor, OUT of scope, forcing-case-gated); today held by read-only/receipt-only design + review. CI now has 4 green jobs: test, gap-status-discipline, receipt-reattestation, witness-boundaries. NEXT PACKET: #7 external/off-LAN gateway-path vantage (witness-position diversity), then #8 pfSense PHP comparator, #9 Kea, #10 TLS 2d-b. D/E (VM root/UMask, backup retention) still household chores. --- PRIOR Packet #5 FAIL-CLOSED RECEIPT RE-ATTESTATION GATE DONE 2026-06-28. Loop back to AUDIT/idle. Built specimens/receipts/ (bounded committed nq.receipt.v1 population: 2 positive specimens from the real engine \u2014 repo_clean/git_status, tests_passed/pytest \u2014 + 4 negative fixtures) + scripts/check-nq-receipts.sh (fail-closed driver, true exit codes) + CI job receipt-reattestation. Reuses nq_core::receipt_check (no new semantics, no re-ratification). Gate green 6/6; proven fail-closed both directions (tamper positive -> exit 1; un-break negative -> exit 1). Coverage boundary NAMED not faked: true past-horizon STALE deferred (verify path emits no freshness_horizon; faking one = re-hashing = the laundering the gate prevents); covered by nq-core unit tests + the freshness_unprovable refusal. Governor loop-receipt bridge deliberately NOT taken. FEATURE_HISTORY entry added; candidate gap doc -> landed. Receipt 2026-06-28T2310Z.receipt-reattestation-gate.json. CI-FLOOR REPAIR DONE 2026-06-28 (ledger repair, no runtime change): cleared the pre-existing gap-status-discipline RED (red on main since 2026-06-09 / b1baafd). Wrote FEATURE_HISTORY \u00a7 WITNESS_CLAIM_SCOPE recording the already-shipped typed-ClaimRefusal migration (cited commits 212332c + 69b4d2d verified real, contract bump 1->2, all 8 constitutional *_cannot_testify functions) + linked the gap doc Status to it. check_gap_status.sh now exit 0. CI floor green before #6. NEXT PACKET: #6 build-graph-enforced witness/probe boundaries (pre-authorized now that #5 landed clean) \u2014 make forbidden witness->mutation/coercion imports structurally impossible; inventory witness/probe crates + mutation surfaces, define forbidden import set, enforce at workspace/build-graph level, CI check proving violation fails, one negative fixture, document allowed exceptions. Doctrine: a witness component should not be able to name the surface that would let it coerce the state it claims to observe. Then #7 external/off-LAN gateway-path vantage, #8 pfSense PHP comparator, #9 Kea, #10 TLS 2d-b. --- PRIOR Packet 4b PUBLIC REVERSE-PROXY SUPERVISION DONE 2026-06-28 (finding B RESOLVED). The public proxy turned out to be a SHARED jbeck-owned docker caddy:2 (real config /home/jbeck/atproto/Caddyfile; ~8 domains incl. nq.neutral.zone -> 172.17.0.1:9848 with a 405 mutation tourniquet) and was ALREADY reboot-survivable (restart=unless-stopped + docker enabled). Added /etc/systemd/system/caddy-proxy.service managing the EXISTING container in place (docker start -a, preserves /data LE certs), container restart=no so systemd is single supervisor, enabled on boot; systemctl restart proven clean; nq local+public 200, leaks 0, no tenant regressed. Reference unit committed deploy/examples/caddy-proxy.service; DEPLOY_CONTRACT finding B -> RESOLVED; receipt 2026-06-28T2025Z.caddy-supervision.json. Operator cleared cross-project ownership. NEXT PACKET: #5 fail-closed receipt re-attestation gate (filed candidate docs/working/gaps/RECEIPT_REATTESTATION_GATE_CANDIDATE.md) \u2014 the doctrine-bearing one; target true nq.receipt.v1 specimens/released receipts FIRST, do NOT bridge .governor/loop-receipts yet. Then #6 build-graph-enforced witness/probe boundaries, #7 external/off-LAN gateway-path vantage, #8 pfSense PHP comparator, #9 Kea, #10 TLS 2d-b. D/E (VM root/UMask, backup retention) remain household chores, not blockers. --- PRIOR Packet #4 DEPLOY STANDARDIZATION DONE 2026-06-27 (Phase 1 inventory+contract AND Phase 2 redeploy). Loop back to AUDIT/idle. FLEET CONVERGED: all three hosts report build_commit=2077dd2e1e2e, schema 58; origin==HEAD==deployed. Coordinated fail-closed redeploy sushi-k(canary,rebuild)->NAS(scp swap)->VM(rsync src incl .git replacing divergent 4ed7c6f fossil + on-host build); VM build_commit None->2077dd2 (the headline fix, end-to-end). Each host verified green first pass (200, render boundaries, zero leak language; VM also public nq.neutral.zone 200). Pre-swap binaries + DB backups retained per host. Receipt 2026-06-28T0335Z.deploy-redeploy.json; contract docs/working/decisions/DEPLOY_CONTRACT.md (criterion #1 PASS; finding C verified-benign). NAMED FOLLOW-UPS (not done, operator-gated): finding B \u2014 public reverse proxy is unmanaged docker caddy:2 (no systemd unit, reboot-survival unverified); finding D \u2014 VM root + UMask 0022 vs 0002 (dedicated nq user candidate); finding E \u2014 backup clutter retention rule. NEXT PACKET: #5 receipt re-attestation gate (filed candidate docs/working/gaps/RECEIPT_REATTESTATION_GATE_CANDIDATE.md) \u2014 now has the stable deploy floor it wanted. Do not free-jazz; open on operator direction. --- PRIOR Phase 1 record: Read-only inventory across all 3 hosts; Read-only inventory across all 3 hosts; doc docs/working/decisions/DEPLOY_CONTRACT.md + receipt 2026-06-28T0305Z.deploy-inventory.json. Named the standard, justified variance (glibc/build-per-host, user-vs-system systemd, /opt+root public host, 0.0.0.0+proxy, host-specific config content), and fixable drift. KEY FINDING: fleet version identity is INCOHERENT \u2014 sushi-k 6a8c443 (on-disk nq-monitor newer/UNDEPLOYED), NAS 6a8c443 (witness byte-identical to sushi-k), VM build_commit=None with src on a DIVERGED commit 4ed7c6f; and 6a8c443..b26c809 carries real code (gateway-path+declared-deny). APPLIED (zero blast radius): VM `git config --global --add safe.directory /opt/notquery/src` (root) \u2014 fixed the build_commit=None root cause (dubious-ownership); effect STAGED until next rebuild. Also found: public reverse proxy is an UNMANAGED docker caddy:2 (no systemd unit, reboot-survival unverified); VM binds 0.0.0.0:9848 (off-box TLS-bypass reachability UNVERIFIED). GATED NEXT (operator go required, public blast radius): coordinated build-per-host redeploy to ONE canonical commit (recommend HEAD b26c809), fail-closed sushi-k->NAS->VM; VM src MUST be re-synced to canonical first (do NOT rebuild the divergent 4ed7c6f tree as-is). Then #5 receipt re-attestation gate. --- PRIOR AUDIT CORRECTION 2026-06-27: (1) docs commit bfa2788 (AGENTS.md -> Lean-kernel formal-spec authority) is now PUSHED \u2014 origin/main = bfa2788 (was stale-local on the deleted agents-md-lean-spec-pointer branch; fast-forwarded into main, branch deleted, pushed). (2) CUSTODY RESOLVED: Packet A custody artifacts live in private repo unpingable/constellation-artifacts (cloned ~/git/constellation-artifacts, clean, 0-ahead/pushed) \u2014 manifests/2026-06-25-pfsense-declared-deny-lab.yaml + receipts/declared-deny-lab-2026-06-25/ + transforms/pfsense-serial-lab/ + README scope-ceiling note. The ~/nqlab/custody-artifacts/ dir is now REDUNDANT local staging scratch (NOT a git repo), superseded by the seeded repo. Custody is OFF the critical path. (3) Receipt re-attestation candidate FILED docs/working/gaps/RECEIPT_REATTESTATION_GATE_CANDIDATE.md (Status: candidate; provenance Lean v1.4.0 Gate 5). Finding: nq HAS a fail-closed per-receipt verifier (nq_core::receipt_check/receipt_replay via `nq-monitor receipt check|replay`) but NO governed driver re-attests a bounded prior-receipt set; nq-verify Action is creation-path only. Parked, NOT in flight. CORRECTED SEQUENCE (operator+ChatGPT): 1 push docs commit [DONE]; 2 lab snapshot/teardown decision [PENDING \u2014 see below]; 3 this governor audit correction [DONE]; 4 deploy standardization across 3 hosts; 5 receipt re-attestation gate (the filed candidate, behind #4). LAB TORN DOWN 2026-06-27 (operator-confirmed): on sushi-k both domains nqlab-pf + nqlab-vantage `virsh undefine --nvram` (were shut off), net nqlab-lan net-destroy+undefine, all disks/isos under ~/nqlab/img/ rm'd (dir now empty; only libvirt `default` net remains). Decommission captured as a receipt in constellation-artifacts: receipts/declared-deny-lab-2026-06-25/teardown-2026-06-27/ (domain+net XML, blklists, img.sha256 pre-deletion; pushed 7b1e775). Disk hashes corroborated the manifest (noble.img 5fa5b05e\u2026, pf-serial.img cf7ff582\u2026 = official sources). Lab is rehydratable from official media + transforms/pfsense-serial-lab/. Local scratch ~/nqlab/custody-artifacts/ + ~/nqlab/teardown-2026-06-27/ remain as harmless redundant staging (superseded by the repo). Do not free-jazz the next packet. PARKED (operator-gated, NOT started): (b) pfSense PHP classification comparator (return_gateways_status / rule status); (c) external/off-LAN vantage for gateway-path; (d) Kea lease parsing; (e) TLS 2d-b operationalization; (f) ~2026-07-12 MANUAL labelwatch re-probe; (g) deploy standardization across the 3 hosts [now sequence #4]. Still referred: series-identity migration; SILENCE OQ3/OQ4; WITNESS_POSITION export-projection; blackbox Bucket 1 promotion; CLOSE-001; OSS Track 1.",
  "batch_plan": [
    "1. inherited dirty tree: admit-or-quarantine (UI slice + reorg path-fixups separately)",
    "2. Lane A: nq-blackbox Bucket 1 probes, then Bucket 3",
    "3. Lane B (operator-authorized): NQ-CLOSE-003 host-trust paragraph; NQ-CLOSE-002 retention-windows decision lock",
    "4. Lane C promotion ANALYSIS only (not implementation) + summary table"
  ],
  "last_verified_commit": "nq: 38d0167 (all three hosts live, schema 63); nq-blackbox: e768f63 (pushed)",
  "last_verified_note": "2026-07-03 daylight deploy: three-host docker fleet (schema 63) + blackbox Bucket 1 prove-the-wheel (sushi-k + VM), all verified by bare exit codes / SMOKE PASS. Two receipts written. Parked at operator request; next levers = identity migration OR step-5 paper layer.",
  "last_updated": "2026-07-03T16:45Z",
  "review_pending_commit": "none \u2014 pfSense lease-presence Phase 1 (3599c85) and Phase 2 (68f9c61) committed; loop closure to follow as a small governor commit. No push.",
  "deferred_operator_directed": [
    "C2 Regime B clock divergence \u2014 DONE 2026-07-01 (host row). Doctrine ratified + host-row dual-explicit markers shipped (Evidence standing primary / Display freshness secondary; host packet observed_at, not worst-over-findings). See FEATURE_HISTORY C2_DUAL_EXPLICIT_HOST_FRESHNESS + DISPLAY_FRESHNESS_VS_ADMISSIBILITY_FRESHNESS.md. Deferred (named): dual marker on service/sqlite-db rows; separately-named Claim-standing (worst-finding) rollup."
  ],
  "session_id": "ag-claude-drives-nq (operator-dispatched, impunity granted: nq-claude stood down)",
  "loop_master": "opus (controller); escalate to fable for doctrine only",
  "wip_limit": 1,
  "dirty_tree_allowed": true,
  "dirty_tree_reason": "inherited orphaned slice from prior nq-claude session (outage/reorg); preserved in .governor/inherited/, being admitted as current slice",
  "re_entry_probes": [
    "cat .governor/loop.json",
    "git -C . log --oneline -8",
    "git -C . status --short",
    "set -o pipefail; cargo test -q -p nq-db -p nq-monitor --no-fail-fast 2>&1 | tail -5; echo EXIT=${PIPESTATUS[0]}"
  ]
}